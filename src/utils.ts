// export const getLocation = async (ipAddress: any) => {
//   const res = await fetch(
//     `https://geo.ipify.org/api/v2/country?apiKey=${process.env.API_KEY}&ipAddress=${ipAddress}`
//   );
//   return res.json();
// };
