import "../Styles/DashboardStyles.css";
const Dashboard = () => {
  return (
    <div className="dashboard-container">
      Style from input styles as placeholder and then fit accordingly with
      dashboard container. <br /> Border radius not done on purpose.
    </div>
  );
};

export default Dashboard;
